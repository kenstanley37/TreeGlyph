<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <!-- 🎚 Default Slider -->
    <Style x:Key="DefaultSliderStyle" TargetType="Slider">
        <Setter Property="MinimumTrackColor" Value="{StaticResource AccentColor}" />
        <Setter Property="MaximumTrackColor" Value="{StaticResource BorderDark}" />
        <Setter Property="ThumbColor" Value="{StaticResource AccentColor}" />
        <Setter Property="HeightRequest" Value="32" />
        <Setter Property="Margin" Value="{StaticResource ControlMargin}" />
        <Setter Property="HorizontalOptions" Value="Fill" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <!-- ⚠️ Warning Slider -->
    <Style x:Key="WarningSliderStyle" TargetType="Slider">
        <Setter Property="MinimumTrackColor" Value="{StaticResource WarningColor}" />
        <Setter Property="MaximumTrackColor" Value="{StaticResource BorderDark}" />
        <Setter Property="ThumbColor" Value="{StaticResource WarningColor}" />
        <Setter Property="HeightRequest" Value="32" />
        <Setter Property="Margin" Value="{StaticResource ControlMargin}" />
        <Setter Property="HorizontalOptions" Value="Fill" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

</ResourceDictionary>