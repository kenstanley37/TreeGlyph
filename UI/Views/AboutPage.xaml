<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TreeGlyph.UI.Views.AboutPage"
             Title="About TreeGlyph"
             BackgroundColor="{StaticResource BaseBackground}">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="16">

            <!-- 🌲 App Name -->
            <Label Text="🌲 TreeGlyph"
                   Style="{StaticResource AppTitle}" />

            <Label Text="Version 1.0"
                   Style="{StaticResource CaptionText}"
                   HorizontalTextAlignment="Center" />

            <BoxView HeightRequest="1"
                     BackgroundColor="{StaticResource DividerColor}" />

            <!-- 📘 What Is TreeGlyph -->
            <Label Text="📘 What Is TreeGlyph?"
                   Style="{StaticResource SectionHeader}" />

            <Label Text="TreeGlyph is a developer-focused utility that converts folder structures into clean, readable ASCII trees. Designed for clarity, portability, and precision — whether you’re documenting a project, cleaning up a repo, or crafting a .gitignore by hand."
                   Style="{StaticResource BodyText}"
                   LineBreakMode="WordWrap" />

            <!-- 🧭 How to Use It -->
            <Label Text="🧭 How to Use It"
                   Style="{StaticResource SectionHeader}" />

            <Label Text="• Select a folder\n• View and customize exclusions\n• Preview the tree structure\n• Copy or save the output\n• Export a .gitignore file if needed"
                   Style="{StaticResource BodyText}"
                   LineBreakMode="WordWrap" />

            <!-- 💡 Tips & Tricks -->
            <Label Text="💡 Tips and Tricks"
                   Style="{StaticResource SectionHeader}" />

            <Label Text="• Hover over folder names to see full paths\n• Use the splitter to resize the preview and source panel\n• Edits in the exclusion box instantly refresh the preview\n• Copy to clipboard with one tap — it's Markdown-safe"
                   Style="{StaticResource BodyText}"
                   LineBreakMode="WordWrap" />

            <BoxView HeightRequest="1"
                     BackgroundColor="{StaticResource DividerColor}" />

            <!-- 🔧 Built With -->
            <Label Text="🔧 Built With"
                   Style="{StaticResource SubsectionHeader}" />

            <Label Text=".NET MAUI • Windows App SDK • MVVM Toolkit • Custom Controls"
                   Style="{StaticResource CaptionText}"
                   HorizontalTextAlignment="Center" />

            <Label Text="Made by Kenneth Stanley — StanleySoft"
                   Style="{StaticResource FooterNote}"
                   HorizontalTextAlignment="Center" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>